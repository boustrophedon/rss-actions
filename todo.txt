only test public interface of cli? i.e. clap/structopt structs as input and command execution functionality

add_feed should just take parameters
feeds table last_updated should be optional so that on first filter it will know to add everything

feeds table alias should be unique


